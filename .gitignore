# Secrets
.env
.env.local

# Next.js
nextjs/node_modules/
nextjs/.next/
nextjs/*.tsbuildinfo
nextjs/next-env.d.ts

# Python cache
__pycache__/
*.pyc

# Cached API results (большие файлы, не нужны в репо)
job_boards/*/results/
job_boards/*.csv
job_boards/cache.json
job_boards/pipeline_cache.json
job_boards/test_log.csv
job_boards/findings.txt
exa/results/
career_pages/data/
enrichment/

# Pipeline run artifacts (triada config/checkpoint/manifest + raw output) — contain
# real contact PII (validation CSVs with emails) and logs, regenerated every run.
pipeline/runs/

# Old stuff
_archive/
